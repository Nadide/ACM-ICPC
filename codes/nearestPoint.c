#include <stdio.h>

struct coordinate {
	double x, y;
} R, L, M;

int main ()
{
	int i;

	scanf ("%lf %lf", &R.x, &R.y);
	scanf ("%lf %lf", &L.x, &L.y);
	scanf ("%lf %lf", &M.x, &M.y);
	
	while (dis(R)-dis(L) > 0.00001) {
		
	
	}
}
