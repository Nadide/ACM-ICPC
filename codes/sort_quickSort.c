#include <stdio.h>

int partition ()
{

}

void qsort (int a[], )
{

}

int main ()
{
	int n, a[1001];

	scanf ("%d", &n);
	for (int i=0; i<n; i++)
		scanf ("%d", &a[i]);

	qsort (a, );	
	for (int i=0; i<n; i++)	
		printf ("%d ", a[i]);
	printf ("\n");
}
